import Colors from './Colors';

const theme = {
    Button: {
        backgroundColor: Colors.primary,
        color: Colors.white,
        // width: 'inherit',
    },
    Text: {
        color: Colors.secondary,
    },
};

export default theme;
